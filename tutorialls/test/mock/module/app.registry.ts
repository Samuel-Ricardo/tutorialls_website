import { INFRA_REGISTRY } from './infra/infra.registry';
import { APPLICATION_REGISTRY } from './application/application.registry';

export const MOCKED_MODULE = {
  APPLICATION: APPLICATION_REGISTRY,
  INFRA: INFRA_REGISTRY,
};
